# สิ่งที่ยังขาด — รายงานบทที่ 5

> อัปเดต **2026-07-17** · ไฟล์ส่ง = `รายงาน_บทที่5_Line-Guard.docx` · ต้นฉบับ = `REPORT_SECTION5.md`
>
> ⚠️ **แก้ที่ `.md` แล้วรัน `python build_docx.py` เสมอ — ห้ามแก้ `.docx` ตรง ๆ**
> (เคยแก้ .md แล้ว .docx ไม่ตาม จนเกือบส่งเวอร์ชันที่ตัวเลขผิดทั้งฉบับ)

---

## 🔴 เหลือ 3 ข้อ — **Claude ทำแทนไม่ได้ ต้องทำเอง**

### 1. หน้าปก — ยังเป็นช่องว่าง 5 จุด
แก้ที่ตัวแปร `COVER` ใน `build_docx.py` แล้ว build ใหม่ (ไม่แต่งชื่อขึ้นเองให้)

- [ ] `[ระบุชื่อ - นามสกุล ผู้พัฒนา]`
- [ ] `[ระบุชื่อทีม / รหัสนักศึกษา]`
- [ ] `[ระบุชื่ออาจารย์ที่ปรึกษา]`
- [ ] `[ระบุชื่อสถาบันการศึกษา]`
- [ ] `[ระบุชื่อโครงการ / การแข่งขัน]` + `ปีการศึกษา [ระบุปี]`

### 2. ฟอนต์ TH Sarabun New — ยังไม่ได้ลงในเครื่อง
ไฟล์ตั้งฟอนต์ถูกแล้ว (ครบทั้ง `w:ascii` / `w:hAnsi` / `w:cs` + `w:szCs`) แต่เครื่องไม่มีฟอนต์ → Word สลับไปใช้ฟอนต์อื่น

- [ ] ลงฟอนต์แห่งชาติ TH Sarabun New แล้วเปิดไฟล์ใหม่
- [ ] เช็กเลขหน้า/การขึ้นหน้าไม่เพี้ยน (จำนวนหน้าจะลดลง เพราะฟอนต์นี้กินที่น้อยกว่า)

### 3. 🖼️ ภาพหน้าจอ demo + Story Board — **ช่องว่างที่ใหญ่ที่สุดที่เหลือ**
ข้อกำหนด 5.1 บังคับว่าต้องมี *"ตัวอย่างโปรแกรม หรือผลงานที่สื่อให้เห็นผลงานที่พัฒนาขึ้น"*
ตอนนี้รายงานบรรยายหน้าจอไว้ **แต่ไม่มีรูปสักรูป** กรรมการจะนึกภาพไม่ออกและดูเหมือนไม่มีของจริง

```bash
python live_demo_offline.py     # ไม่ต้องใช้ GPU ไม่ต้องเช่า Vast
# เปิด http://localhost:5004 -> PrintScreen
```

- [ ] แคป 3 จังหวะ: (ก) ตอนการ์ด CATCH เด้ง (ข) panel SOP Overview (ค) ตอนคลิกดูรูปหลักฐาน
- [ ] Story Board (5.1.3) เป็นตารางล้วน — ดึงเฟรมจริง 4–6 เฟรมมาเรียงเป็นแถบ
      เหตุการณ์ที่ควรมีภาพ: ของหล่น 00:14 · คนนอก 01:36 · **near-miss 02:45** · ไม่สวมหมวก 03:00
      (เฟรมมีครบใน `_frames_full/`) — ครอบกรอบแดงตรงจุดเกิดเหตุจะอ่านง่ายขึ้นมาก

> 🤖 **Claude ลองถ่ายให้แล้วไม่สำเร็จ (17 ก.ค.)** — demo ทำงานปกติ (ยืนยันด้วยการอ่าน DOM แล้ว
> เห็น SOP panel + event ครบ) แต่ตัวจับภาพของ browser tool timeout ทุกครั้ง แม้หยุดวิดีโอ
> และถอด `<video>` ออกแล้ว = ข้อจำกัดของเครื่องมือ ไม่ใช่ demo พัง

---

## 🟢 ตามข้อกำหนดแต่ยังไม่ได้เขียน (คนละเอกสาร)

ข้อกำหนดระบุว่าต้องมี 3 อย่าง ใช้ฟอร์แมตเดียวกัน (TH Sarabun New 16pt, A4, ขอบ 1 นิ้ว, มีเลขหน้า)

- [x] **รายงานฉบับสมบูรณ์** — บทที่ 5 เสร็จแล้ว
- [ ] **คู่มือการติดตั้ง** — ข้อมูลมีครบแล้ว (serve Cosmos, ตั้ง tunnel, gotchas จริงที่เจอ) แค่ยังไม่ได้เขียน
- [ ] **คู่มือการใช้งาน** — ข้อมูลมีครบแล้ว (รัน demo 2 โหมด, อ่านผล, ดู evidence frame)

> บทที่ 1–4 และ 6 อยู่นอกขอบเขตที่คุยกัน — ถ้าต้องเขียนด้วยบอกได้

---

## ✅ เคลียร์แล้ว (ข้อที่ค้างในฉบับ 2026-07-16)

| ข้อเดิม | สถานะปัจจุบัน |
|---|---|
| **6. ไม่มี References เลยสักรายการ** | ✅ **เพิ่ม 9 รายการ** เปิดตรวจ URL จริงทุกตัว · ตัวสำคัญคือ **[9] ARO benchmark** (Yuksekgonul et al., ICLR 2023) ที่รองรับข้อค้นพบเรื่อง attribute binding · **Cosmos-Reason2 ไม่มีบทความของตัวเอง** (ตรวจแล้ว) จึงอ้าง model card + บทความรุ่นก่อน โดยระบุชัดว่าคนละรุ่น |
| **5. License ยังไม่ได้เปิดเว็บยืนยัน** | ✅ ตรวจครบ 3 ตัว — **พบว่า Cosmos เป็นสัญญาอนุญาตคู่** (NVIDIA Open Model License **+ Apache-2.0**) ฉบับเดิมระบุไม่ครบ แก้แล้ว · vLLM Apache-2.0 ✅ · `eugenesiow/edsr-base` Apache-2.0 ✅ |
| **7. เทียบ Qwen3-VL อ้างว่าชนะแต่ไม่มีตัวเลข** | ✅ มีตารางใน **5.2** แล้ว: Qwen 62% (5/8) ไม่พบ near-miss · Cosmos 75% (6/8) พบ |
| **8. เวอร์ชันเครื่องมือไม่ครบ** | ✅ ครบใน 5.3 + 5.6 — Python 3.12.13 · PyTorch 2.11.0+cu130 · CUDA 13.0 · vLLM 0.25.0 (จาก `env_report.json`) |
| **9. สเปกเครื่องไม่ครบ** | ✅ ครบใน 5.6 — RTX 4090 ×2 (24,564 MiB) · ไดรเวอร์ 595.71.05 · AMD EPYC 7282 (32 threads) · RAM 125 GB |
| **10. F5 จับคู่ได้ 10/11 แล้วอีก 1 คืออะไร** | ✅ แก้ event ถุงมือให้ตรงเฟรมจริงแล้ว → **11/11** ตรงกับ `cam_a05_timeline.json` |
| **11. เฉลยไม่ครบทุกเฟรม (34%)** | ✅ ระบุเป็นข้อจำกัดตรงไปตรงมาแล้ว · **ไม่ต้องขยายเป็น 220 เฟรม** — คอขวดคือคนตรวจ ไม่ใช่ GPU และจุดอ่อนจริงคือ N=1 (มีคลิปเดียว) ถ้ามีเวลาให้ไปถ่ายคลิปที่ 2 คุ้มกว่า |
| ตัวเลขความแม่นยำในรายงานผิด | ✅ วัดใหม่บนเฉลย v4 → **82% (46/56) FP 5** · `python diff_gt.py` พ่นเลขตรงทุกตัว |
| ตาราง 5.4.6 อ้าง `claude_gt.json` (เฉลย v1 ที่ผิด 32 จุด) | ✅ แก้เป็น `claude_gt_v4.json` |

## 📌 หมายเหตุ
`REPORT_DATA_ISSUES.md` เป็นบันทึก**ตอนสำรวจปัญหา** (16 ก.ค.) ประเด็นส่วนใหญ่แก้ไปแล้ว
สถานะปัจจุบันให้ดู **`MEASUREMENTS.md`** (ตัวเลข) และ **`RESUME_HERE.md`** (งานค้าง) แทน

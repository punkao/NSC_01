# สิ่งที่ยังขาด — รายงานบทที่ 5

> เช็คลิสต์ข้อมูลที่ต้องไปหา/ทำเพิ่ม ก่อนส่งรายงาน
> อัปเดต 2026-07-16 · ไฟล์รายงาน = `รายงาน_บทที่5_Line-Guard.docx` · ต้นฉบับ = `REPORT_SECTION5.md`

---

## 🔴 ต้องทำก่อนส่ง (ขาดแล้วเสียคะแนน / ผิดข้อกำหนด)

### 1. หน้าปก — ยังเป็นช่องว่าง 5 จุด
ผมไม่แต่งชื่อขึ้นเอง ต้องกรอกเอง

- [ ] `[ระบุชื่อ - นามสกุล ผู้พัฒนา]`
- [ ] `[ระบุชื่อทีม / รหัสนักศึกษา]`
- [ ] `[ระบุชื่ออาจารย์ที่ปรึกษา]`
- [ ] `[ระบุชื่อสถาบันการศึกษา]`
- [ ] `[ระบุชื่อโครงการ / การแข่งขัน]` + `ปีการศึกษา [ระบุปี]`

### 2. ฟอนต์ TH Sarabun New — ยังไม่ได้ลงในเครื่อง
ไฟล์ตั้งชื่อฟอนต์ไว้ถูกแล้ว แต่เครื่องนี้ไม่มีฟอนต์ → Word สลับไปใช้ฟอนต์อื่น

- [ ] โหลดฟอนต์แห่งชาติ TH Sarabun New มาลง แล้วเปิดไฟล์ใหม่
- [ ] เช็กว่าเลขหน้า/การขึ้นหน้าไม่เพี้ยน (จำนวนหน้าจะลดจาก 16 เพราะฟอนต์นี้กินที่น้อยกว่า)

### 3. 🖼️ ไม่มีภาพหน้าจอ demo เลย — ช่องว่างที่ใหญ่สุด
ข้อกำหนด 5.1 เขียนชัดว่าต้องมี **"ตัวอย่างโปรแกรม หรือผลงานที่สื่อให้เห็นผลงานที่พัฒนาขึ้น"**
ตอนนี้รายงานบรรยายว่า "วิดีโอฝั่งซ้าย แจ้งเตือนฝั่งขวา" **แต่ไม่มีรูปให้ดูสักรูป**
กรรมการอ่านแล้วจะนึกภาพไม่ออก และดูเหมือนไม่มีของจริง

- [ ] รัน `live_demo.py` แล้วแคปหน้าจอตอนมีแจ้งเตือนเด้ง (เอาช่วง near-miss 02:45 หรือ PPE 03:00)
- [ ] แคปตอนมีการ์ดแจ้งเตือนหลายใบเรียงกัน จะเห็นผลงานชัดสุด
- [ ] ⚠️ ต้องมี GPU รัน Cosmos ถึงจะแคปโหมดสดได้ — **ถ้าเครื่อง Vast ปิดไปแล้วต้องเช่าใหม่**
      (ทางเลือก: ใช้ `live_demo_offline.py` ที่อ่านจาก JSON ไม่ต้องใช้ GPU → แคปได้เลย หน้าตาเหมือนกัน)

### 4. 🖼️ Story Board ไม่มีภาพจริงจากวิดีโอ
5.1.3 เป็นตารางล้วน ทั้งที่ story board ควรมีภาพ

- [ ] ดึงเฟรมจริง 4–6 เฟรมจากเหตุการณ์สำคัญมาเรียงเป็นแถบ
      (มีของอยู่แล้วใน `phase0_frames/` และ `_frames/`)
- [ ] เหตุการณ์ที่ควรมีภาพ: ของหล่น 00:14 · คนนอก 01:36 · **near-miss 02:45** · ไม่สวมหมวก 03:00
- [ ] ถ้าใส่ได้ ให้ครอบกรอบสีแดงตรงจุดที่เกิดเหตุ จะอ่านง่ายขึ้นมาก

### 5. ยืนยันสัญญาอนุญาต (License) ใน 5.4.6
ผมกรอกจากที่รู้ **ยังไม่ได้เปิดเว็บยืนยัน** — ถ้าผิดในรายงานวิชาการจะเสียหาย

- [ ] Cosmos-Reason2-8B → เช็กหน้า model card บน Hugging Face (ผมใส่ "NVIDIA Open Model License")
- [ ] vLLM → เช็ก LICENSE ใน GitHub (ผมใส่ Apache-2.0)
- [ ] super-image / EDSR → เช็ก (ผมใส่ Apache-2.0)

---

## 🟡 ควรมี (ทำให้รายงานน่าเชื่อขึ้นชัดเจน)

### 6. ไม่มีการอ้างอิง (References) เลยสักรายการ
รายงานระดับนี้ที่อ้างทฤษฎี (ViT patching, attention, PagedAttention, EDSR) แต่ไม่มี citation
จะโดนถามแน่ว่าเอาข้อมูลมาจากไหน

- [ ] Cosmos-Reason (NVIDIA) — paper / model card
- [ ] vLLM — PagedAttention paper (Kwon et al., 2023)
- [ ] ViT — Dosovitskiy et al., 2020 (ที่มาของ patch 14×14)
- [ ] EDSR — Lim et al., 2017
- [ ] Qwen3-VL — model card (ที่เอามาเทียบ)

### 7. เทียบกับ Qwen3-VL — อ้างว่าชนะ แต่ไม่มีตัวเลข
5.2.1 เขียนว่า "เลือก Cosmos เพราะให้คำตอบเสถียรกว่า" = **ความเห็น ไม่ใช่หลักฐาน**
เป็นจุดอ่อนที่กรรมการจิ้มได้ง่าย

- [ ] หา log/ผลการเทียบ Qwen3-VL ที่เคยรัน (ลองหาใน `WORKLOG.md`)
- [ ] ถ้าไม่มีตัวเลข → **แก้ข้อความให้ตรงความจริง** ว่าเป็นการประเมินเชิงคุณภาพจากการทดลองใช้
      (ซื่อสัตย์กว่าปล่อยให้ดูเหมือนมีการวัด)

### 8. เวอร์ชันเครื่องมือยังไม่ครบ (5.3)
เพิ่ง fix vLLM ให้แล้ว (0.23.0 / 0.25.0) ที่เหลือยังว่าง — จำเป็นต่อ reproducibility

- [x] vLLM = 0.23.0 (RTX 3090) → 0.25.0 (RTX 4090) ✅ ใส่แล้ว
- [ ] Python เวอร์ชันบนเครื่อง GPU
- [ ] PyTorch (เห็นใน WORKLOG ว่า ultralytics เผลออัปเป็น torch 2.13 — ต้องยืนยันว่าตอนรันจริงใช้เวอร์ชันไหน)
- [ ] CUDA / NVIDIA driver
- [ ] ⚠️ ข้อมูลนี้อยู่บนเครื่อง Vast — **ถ้าปิดไปแล้วจะหาไม่ได้อีก**

### 9. สเปกเครื่องไม่ครบ (5.6)
มีแต่ GPU ยังขาดส่วนอื่นของเครื่อง

- [ ] CPU รุ่นอะไร กี่คอร์
- [ ] RAM เท่าไหร่
- [ ] ⚠️ อยู่บนเครื่อง Vast เหมือนกัน

### 10. F5 บอกว่า "จับคู่ได้ 10 จาก 11 เหตุการณ์" — แล้วอีก 1 คืออะไร
ตัวเลขนี้ค้างคาอยู่ ไม่ได้บอกว่าเหตุการณ์ไหนที่จับคู่ไม่ได้ และทำไม

- [ ] เปิด `analyze_safety.py` / ผลลัพธ์ ดูว่าตัวไหนหลุด แล้วเขียนเหตุผลสั้น ๆ

### 11. เฉลยยังไม่ครบทุกเฟรม (34%)
เพิ่งแก้ข้อความให้ตรงความจริงแล้ว และระบุเป็นข้อจำกัดไว้

- [ ] (ถ้ามีเวลา) ขยาย `claude_gt.json` ให้ครบ 220 เฟรม → ตัวเลขจะน่าเชื่อขึ้นอีกขั้น
- [ ] ถ้าไม่ทำ ก็ปล่อยไว้ได้ — ตอนนี้รายงานบอกข้อจำกัดนี้ตรงไปตรงมาแล้ว

---

## 🟢 ตามข้อกำหนดแต่ยังไม่ได้เขียน (คนละเอกสาร)

ข้อกำหนดระบุว่าต้องมี 3 อย่าง ใช้ฟอร์แมตเดียวกัน (TH Sarabun New 16pt, A4, ขอบ 1 นิ้ว, มีเลขหน้า)

- [x] **รายงานฉบับสมบูรณ์** — บทที่ 5 เสร็จแล้ว
- [ ] **คู่มือการติดตั้ง** — ข้อมูลมีครบแล้ว (serve Cosmos, ตั้ง tunnel, gotchas จริงที่เจอ) แค่ยังไม่ได้เขียน
- [ ] **คู่มือการใช้งาน** — ข้อมูลมีครบแล้ว (รัน demo 2 โหมด, อ่านผล, ดู evidence frame)

> บทที่ 1–4 และ 6 อยู่นอกขอบเขตที่คุยกัน — ถ้าต้องเขียนด้วยบอกได้

---

## ✅ ข้อมูลที่ครบแล้ว ไม่ต้องหาเพิ่ม

| หัวข้อ | สถานะ |
|---|---|
| 5.2 ทฤษฎี (VLM, tokenization, prefill/decode, data-parallel) | ครบ + มีรูปประกอบ 2 รูป |
| 5.4.3 Input/Output spec | ครบ (มี JSON schema จริง) |
| 5.4.4 Functional spec F1–F8 | ครบ + มีหลักฐานตัวเลขทุกข้อ |
| 5.4.5 กฎ R1–R10 | ครบ |
| 5.4.6 ส่วนที่พัฒนาเอง | ครบ (map กับไฟล์จริง) |
| 5.5 ข้อจำกัด | ครบและแข็ง — มีทั้งตัวเลข สาเหตุ และการพิสูจน์ |
| 5.6 เทียบฮาร์ดแวร์ | ครบ (วัดจริง 4 คอนฟิก) |

---

## ⚠️ เรื่องที่ค้างมานาน (ไม่เกี่ยวรายงาน แต่ควรทำ)

- [ ] **เพิกถอน Hugging Face token** ที่เคยวางไว้ในแชต (บัญชี Falsity24, เก็บที่ `/workspace/.hf_env`)
      — token ที่หลุดออกจากเครื่องแล้วถือว่าใช้ไม่ได้อีก ต้องสร้างใหม่
- [ ] เช็กว่าเครื่อง Vast ยังเปิดอยู่ไหม (ถ้าเปิดค้าง = เสียเงินฟรี / ถ้าปิดแล้ว = ข้อมูลข้อ 8, 9 หายแล้ว)

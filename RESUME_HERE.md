# ▶️ ทำต่อจากตรงนี้ (2026-07-17)

> เขียนไว้ตอน limit ใกล้หมด + กำลังจะปิด vast
> **อ่านไฟล์นี้ก่อนเริ่ม แล้วค่อยไป `MEASUREMENTS.md` / `DATA_INDEX.md`**

---

## ✅ งานค้าง #1 — **เสร็จแล้ว 2026-07-17: เฉลยถูก ตัวเลขไม่เปลี่ยน**

ตรวจ FP ถุงมือครบทั้ง 3 จุดแล้ว (zoom 3× + ดูเฟรมข้างเคียง ±8 วิ) → **เฉลยถูกทั้งหมด FP เป็นของจริง**
**ถุงมือคงเดิม 4/7 = 57% · FP 3 · รวม 46/56 = 82%** — ไม่ต้องแก้ไฟล์ใด ๆ

รายละเอียด + สาเหตุความผิดพลาดทั้ง 2 โหมด → `MEASUREMENTS.md` §2.9 (บันทึกลงรายงาน + .docx แล้ว)
ภาพหลักฐาน: `_audit_gloves/`

**สรุปสาเหตุ:** โมเดลตอบ "ใส่ถุงมือไหม" ที่ **ระดับคน ไม่ใช่ระดับมือ** → ถอดข้างเดียวหรือมือถูกผ้าแดงบัง = พัง
**ทางแก้ถ้าจะทำต่อ:** แยก probe รายมือ (แนวเดียวกับ ROI ที่แก้ปัญหาระบุลัง) — **ไม่ใช่แก้ prompt**

---

## 🟡 งานค้าง #2 — "รันสดเช็คทั้งวิดีโอ" (ที่คุยค้างไว้) — **คิดให้ดีก่อนเช่า GPU ใหม่**

**คอขวดไม่ใช่ GPU — คือคนตรวจ**

- รัน Cosmos 220 เฟรม = **~2 นาที GPU (ถูกมาก)**
- แต่เฉลยต้องมีคน**ไล่ดูด้วยตา 220 เฟรม × 3 อย่าง × 2 คน = 1,320 การตัดสิน** ← อันนี้แหละที่แพง

**เฉลยตอนนี้ = 74 จุด (ทุก 3 วิ)** ซึ่ง**พอสำหรับ N=1 อยู่แล้ว** การขยายเป็น 220 จุดไม่ได้ทำให้ข้อสรุปเปลี่ยน (เพราะจุดอ่อนจริงคือ **มีคลิปเดียว** ไม่ใช่ sample น้อย)

> **ถ้ามีเวลา/เงินเหลือ ให้ไปถ่ายคลิปที่ 2 ดีกว่า** — แก้จุดอ่อนใหญ่สุด (N=1) และ**ไม่ต้องใช้ GPU ในการถ่าย**

---

## ✅ เก็บกวาดเพิ่ม (2026-07-17 รอบเดียวกัน)

- **`python diff_gt.py` เฉย ๆ ต้องได้ 82% / FP 5** — เดิมพ่น 46% เพราะอ่านไฟล์เดียว (ตกหน้ากาก SR)
  = คำสั่งในเอกสารทำซ้ำเลขในเอกสารไม่ได้ **แก้แล้ว** (เพิ่ม `--mask-obs`, default = คอนฟิกที่ส่งมอบ)
  ถ้าได้ 46% แปลว่า `image_obs_sr.json` หาย → รัน `som_sr_ppe.py`
- **ติดป้าย 🛑 ให้เอกสารเก่าที่ตัวเลขผิด** — `MODEL_HANDOFF_TO_TEAM.md` (อันตรายสุด จ่าหน้าให้เพื่อนอ่าน
  แต่เขียนถุงมือ 100% / recall 100% / precision 91%), `HANDOFF.md`, `PROMPT_FOR_FRIEND.md`, `WORKLOG.md`
- `grade.py` เคยชี้ `claude_gt.json` (เฉลย v1 ผิด 32 จุด) → ชี้ v4 แล้ว · `claude_gt_v3.json` → `_archive/`
- `DATA_INDEX.md` เคยบอกว่าใช้เฉลย v3 (53 จุด) → แก้เป็น v4 (56 จุด)

## ✅ ที่เสร็จแล้ว ไม่ต้องแตะ
- `cam_a05_timeline.json` — 220 narration + 11 events (rule_id 11/11) + SOP ระดับลัง · **ตัวเลขตรงกับเอกสารทุกไฟล์แล้ว**
- `PROMPT_FOR_FRIEND_UPDATE.md` — ตรวจแล้วเลขตรง JSON ทุกตัว · ส่งเพื่อนได้เลย
- `.docx` — สร้างจาก `build_docx.py` แล้ว (**ห้ามแก้ .docx ตรง ๆ — แก้ .md แล้ว build ใหม่**)

## 📌 ยังไม่ได้ทำ (ไม่ใช่โค้ด)
- ติดตั้งฟอนต์ TH Sarabun New บนเครื่องตัวเอง ไม่งั้น .docx ขึ้นฟอนต์สำรอง
- เติมหน้าปก 5 ช่อง `[ระบุ...]`
- ใส่ screenshot demo + story board (ข้อกำหนด 5.1 บังคับ) — `live_demo_offline.py` **ไม่ต้องใช้ GPU**
- ใส่ References (ยังไม่มีเลย): Cosmos, vLLM/PagedAttention, ViT, EDSR, Qwen3-VL

## ⚠️ ห้ามลืม
- **ปิด/destroy vast ทุกเครื่อง** (71.104.167.38:54728 · 152.160.24.154:10378)
- **revoke HF token** หลัง demo
- ถ้าเช่า GPU ใหม่ → **รัน `python3 preflight.py` ก่อนเสมอ** (template vast ตั้ง `VLLM_MODEL=Qwen` ทับ เคยวัดผิดรุ่นมาแล้ว)

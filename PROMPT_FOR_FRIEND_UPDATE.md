# 📋 Prompt ส่งให้ Claude ของเพื่อน (frontend)

> **วิธีใช้:** ก๊อปทุกอย่างในบล็อกด้านล่าง (ตั้งแต่ `===` ถึง `===`) ไปวางใน Claude ของเพื่อน
> อัปเดตล่าสุด 2026-07-17 — ฉบับนี้แทนที่ของเก่าทั้งหมด

---

```
=== เริ่มก๊อปจากตรงนี้ ===

ฝั่ง model อัปเดตใหญ่ pull ล่าสุดก่อน แล้วอ่าน 2 ไฟล์นี้ให้จบก่อนแตะโค้ด:
  spike/INTEGRATION_FOR_FRONTEND.md   (ข้อ 2 = แตกเฟรม, ข้อ 9 = SOP ระดับลัง, ข้อ 10 = ความแม่นยำจริง)
  spike/MEASUREMENTS.md               (ตัวเลขทุกตัว + คำสั่งทำซ้ำ)

## สรุปสิ่งที่เปลี่ยน และทำไม

### 1) VLM ระบุ "ชื่อลัง" เองไม่ได้ — แก้แล้วด้วยวิธีใหม่
เจอว่า Cosmos บอก "หยิบจากลัง NG" ผิด 146/220 จุด (66%) ทั้งที่จริงหยิบจากลัง WAIT
สาเหตุ: ลัง WAIT/NG วางชิดกันแค่ 251px -> โมเดลจับป้ายผิดลัง (attribute-binding failure)
  หลักฐาน: ลัง GOOD ที่อยู่เดี่ยวห่าง 1053px โมเดลไม่เคยเรียกผิดเลย
  = ไม่ใช่ "อ่านป้ายไม่ออก" แต่ "จับคู่ป้ายผิดลัง"
แก้ด้วย prompt 4 วิธีแล้วไม่ผ่านทุกวิธี -> เลิกให้ VLM อ่านป้าย

วิธีที่ใช้แทน (late fusion):
  - โค้ด crop ทีละลัง (กล้องนิ่ง ลังอยู่กับที่) -> "ลังไหน" มาจากโค้ด 100%
  - VLM ตอบแค่ "มีมือในกรอบนี้ไหม" -> ตรง 8/8 เทียบการไล่ดูเฟรมด้วยคน (ฐานเล็ก: 4 จุดเวลา)
  - แล้วประกอบเป็นประโยค: "หยิบกระป๋องจากลัง WAIT"
=> narration ใน JSON บอกชื่อลังถูกต้องแล้ว ใช้ field `narration` ได้ตามปกติ

### 2) เฉลยที่ใช้วัดผลเดิม "ผิด" — ตัวเลขความแม่นยำเปลี่ยนหมด
เฉลยชุดแรกทำโดยให้โมเดลอีกตัวดูภาพแล้วตัดสิน และไม่เคยมีใครตรวจสอบ
ไล่ดูเฟรมจริงทุกจุดแล้วพบว่าเฉลยผิด 32 จุด (ส่วนใหญ่คือหน้ากาก)
=> ทำเฉลยใหม่ `claude_gt_v4.json` (บันทึกทุกจุดที่แก้ไว้ในฟิลด์ `corrected`)

### 3) event แจ้งเตือนเปลี่ยนเวลา
  หน้ากากคนที่ 1:  02:12-03:06 + 03:33-03:39  ->  02:00-02:54 + 03:09-03:39
  ถุงมือคนที่ 2:   02:12-02:24                ->  02:09-02:15
  (ของเดิมมาจากคอนฟิกที่พิสูจน์แล้วว่าแย่ + span ไม่ตรงกับภาพจริง)
=> ถ้า UI hardcode เวลาไว้ที่ไหน ต้องเอาออก ให้อ่านจาก JSON อย่างเดียว

## สิ่งที่ต้องทำใน frontend

1. narration (field `narration`) ใช้ได้เลย ไม่ต้องแก้ — บอกชื่อลังถูกต้องแล้ว
2. events มี 11 รายการ ครบทุกอันมี rule_id + severity + evidence_frame (11/11)
3. เพิ่ม UI รองรับ field ใหม่ 3 ตัว:
   - `box_activity`  : { "<วินาที>": { "WAIT": bool, "NG": bool } } -> timeline bar ว่ามือยุ่งลังไหน
   - `sop_events`    : 32 รายการ (WAIT 28 + NG 4) แต่ละอันมี needs_review + evidence_frame
   - `sop_findings`  : สรุป (wait_interactions=28, ng_interactions=4, ng_review_times=[...])
4. ทำ panel "จุดที่ต้องให้คนตรวจสอบ" — โชว์ sop_events ที่ needs_review=true (4 จุด: 00:10, 01:46, 03:04, 03:28)
   โชว์ evidence_frame ให้กดดูได้ + ปุ่มให้คนยืนยัน (คนตัดสิน ไม่ใช่ AI)
5. `sop_compliance` (field เก่า) ถูกยกเลิก ถ้า UI อ้างอยู่ให้เอาออก -> ใช้ `sop_overview` + `sop_events` แทน

## ความแม่นยำจริง — ห้าม UI/สไลด์ เคลมเกินนี้

วัดกับเฉลยที่ไล่ตรวจเฟรมจริงครบทุกจุด (74 จุดตรวจ พบการละเมิดจริง 56 จุด):

  หมวก    100% (14/14)  แจ้งผิด 0    <- เห็นชัด แม่นที่สุด
  ถุงมือ    57% (4/7)   แจ้งผิด 3    <- พลาดตอนถอดข้างเดียว / ผ้าแดงบังมือ
  หน้ากาก   80% (28/35) แจ้งผิด 2    <- ต้องใช้ super-resolution
  รวม       82% (46/56) แจ้งผิด 5

ห้ามเขียน: "แม่นยำ 100%" (จริงเฉพาะหมวก) · "ไม่มีการแจ้งผิดเลย" (มี 5 จุด)
เขียนได้:  "ตรวจพบ 82% ของการละเมิดทั้งหมด · หมวกแม่น 100% · ถุงมือกับหน้ากากยังพลาดได้เมื่อถูกบัง"

## กฎเหล็กอีก 2 ข้อ

1. อย่าเขียนว่า "หยิบออกจากลัง NG" — ระบบรู้แค่ "มีมือมายุ่งกับลัง" แยกไม่ได้ว่าใส่ลงหรือหยิบออก
   (ลองวัดปริมาณของในลังด้วย pixel แล้ว เงาคน/กล้องปรับแสงทำค่าแกว่งแรงกว่าสัญญาณจริง 3 เท่า
    + คลิปมีรอยตัดต่อ ~02:17 ที่ของในลังเปลี่ยนเพราะจัดฉากใหม่)
2. อย่าดึงชื่อลังจากข้อความ narration ไปทำ logic -> ใช้ `box_activity` แทน

## เรื่องเฟรม (เจ็บมาแล้ว 2 รอบ — ห้ามพลาด)

เฟรมไม่ได้อยู่ใน git ต้องแตกเอง และต้องใช้คำสั่งนี้เป๊ะ:

  ffmpeg -i cam_a05_1080p.mp4 -vf fps=1 -start_number 0 -q:v 2 _frames_full/t%04d.jpg

- ขาด -start_number 0 -> ffmpeg เริ่มที่ t0001 -> รูปหลักฐานเลื่อน 1 วินาทีทั้งไฟล์
- ต้องใช้ไฟล์ 1080p ไม่ใช่ 720p (720p ใช้เล่นในหน้าเว็บเท่านั้น)
- ห้ามใช้โฟลเดอร์ _frames/ เก่า (มี 175/220 + จังหวะไม่ตรง)
เช็ค: ls _frames_full | wc -l ต้องได้ 220 (t0000.jpg ถึง t0219.jpg)

## ตรวจสอบย้อนหลังได้ (เผื่อกรรมการถาม "AI รู้เอง หรือใส่ให้?")

ทุก entry ใน timeline มี:
  narration_vlm : คำพูดดิบของ Cosmos (ก่อนประกอบ)
  narration     : ที่ demo โชว์ (ประกอบชื่อลังจาก ROI แล้ว)
  box_source    : ชื่อลังนี้มาจากไหน
                  "WAIT:roi" = ระบบตรวจเจอเอง | "GOOD:layout" = จริงตามผังกล้อง (ฝั่งขวามีลังเดียว)
                  "left:ambiguous" = มือคร่อม 2 ลัง ระบบไม่เดา | "left:no_hand" = ไม่เห็นมือในลัง

## ข้อจำกัดที่ยังมี (เขียนในสไลด์ได้เลย = จุดแข็ง ไม่ใช่จุดอ่อน)

- ระบบ verify ได้ว่า "มือยุ่งกับลังไหน" แต่ verify ไม่ได้ว่า "หยิบออก หรือ ใส่ลง"
  -> ออกแบบเป็น "AI คัดกรอง -> คนตัดสิน" (ย่อวิดีโอ 3.5 นาที เหลือ 4 จุดที่ต้องดู)
- narration รันสด 2 ครั้งได้ถ้อยคำไม่เหมือนกันเป๊ะ (vLLM ไม่ deterministic แม้ temperature=0
  เพราะ continuous batching) แต่ชั้นข้อเท็จจริง (ชื่อลัง/severity/การตรวจจับ) นิ่ง 100%
  เพราะไม่ได้พึ่งการพ่นข้อความ -> ถ้ากรรมการถามว่าทำไมรัน 2 รอบไม่เหมือนกัน ตอบข้อนี้
- event หน้ากากยังแจ้ง 2 ครั้ง (02:00-02:54, 03:09-03:39) ทั้งที่ความจริงถอดต่อเนื่อง 01:57-03:39
  เพราะโมเดลพลาดช่วง 02:57-03:06 จริง ๆ — ไม่ได้จูนให้รวมเป็นก้อนเดียวเพราะนั่นคือการปั้นตัวเลข

ถ้าสงสัยตรงไหน ถามกลับมา อย่าเดาเอง

=== ก๊อปถึงตรงนี้ ===
```

---

## 💬 ข้อความสั้นส่งเพื่อนในแชท (ให้เพื่อนอ่านเอง ไม่ใช่ให้ Claude)

> เฮ้ย **pull ล่าสุดด้วย** ฝั่ง model อัปเดตใหญ่
>
> **1. เจอบั๊กใหญ่:** Cosmos บอก *"หยิบจากลัง NG"* ทั้งที่จริงคือ **WAIT** — ผิด **66%** ของคลิป
> แก้แล้วด้วยการ**ให้โค้ด crop ทีละลัง** แทนที่จะให้ AI อ่านป้ายเอง (ทดสอบแล้วตรง 8/8)
>
> **2. เจอเรื่องใหญ่กว่า:** **เฉลยที่เราใช้วัดผลมาตลอด มันผิดเอง 32 จุด** 😱
> (เฉลยทำโดยให้ AI อีกตัวดูภาพ แล้วไม่เคยมีใครตรวจ) → กูไล่ดูเฟรมจริงทุกจุดแล้วทำเฉลยใหม่
> **ตัวเลขความแม่นยำเลยเปลี่ยนหมด → 82%** (หมวก 100% / ถุงมือ 57% / หน้ากาก 80%)
>
> **3. เวลา event เปลี่ยน** → ถ้า UI hardcode เวลาไว้ **เอาออก อ่านจาก JSON อย่างเดียว**
>
> **⚠️ สำคัญสุด:** ในคู่มือเก่าเขียนว่า **"แม่น 100%"** — **ไม่จริง** ห้ามเอาไปขึ้นใน UI/สไลด์
> ของจริงคือ **82%** เขียนตามนี้ปลอดภัยกว่า ถ้ากรรมการให้รันโชว์แล้วเลขไม่ตรงคือจบ
>
> **วิธีอัปเดต:** เปิด `spike/PROMPT_FOR_FRIEND_UPDATE.md` ก๊อปบล็อกในนั้นไปวางใน Claude มึงเลย มีบอกครบ

---

## 📎 หมายเหตุสำหรับเรา (ไม่ต้องส่ง)

| ไฟล์ | หน้าที่ |
|---|---|
| `claude_gt_v4.json` | เฉลยปัจจุบัน (v1-v3 อยู่ใน `_archive/ground_truth/`) |
| `roi_scan.py` → `sop_roi.py` → `compose_narration.py` | ท่อ ROI (ระบุลัง) |
| `rebuild_mask_events.py` | สร้าง event หน้ากากจาก SR ไม่มี gate |
| `preflight.py` | กัน env ของเครื่องเช่าทำให้ยิงผิดโมเดล |
| `build_docx.py` | สร้างรายงาน .docx จาก .md (แก้ .md แล้วรันใหม่) |
| `MEASUREMENTS.md` | **ตัวเลขทุกตัวต้องมาจากที่นี่** |

---

## 🖼️ อัปเดต 2026-07-17 — แก้ภาพหลักฐาน 2 เหตุการณ์ (ขอบคุณที่ทัก!)

**เรื่องนี้เพื่อนเป็นคนเจอ** — ทักว่าภาพหลักฐาน near-miss ที่ 02:45 ไม่เห็นมีอะไรใกล้เคียงกับที่แจ้งเตือน
ตรวจแล้ว **จริง** และเป็นบั๊กเชิงระบบ

| เหตุการณ์ | `evidence_frame` เดิม | ใหม่ | ทำไม |
|---|---|---|---|
| near-miss 02:45 | `t0165.jpg` | **`t0175.jpg`** | 02:45 ทั้งสองคนอยู่ที่ลัง ไม่มีมือใกล้เครื่อง · เหตุจริงอยู่ที่ **02:55** |
| บุคคลภายนอก 01:36 | `t0096.jpg` | **`t0099.jpg`** | 01:36 คนนอกถูกคนที่ 1 บังเกือบมิด · 01:39 ยืนเต็มตัวกลางภาพ |

**สาเหตุ:** โปรแกรมเลือก "เฟรมแรกของช่วง" เป็นภาพหลักฐานเสมอ — ใช้ได้กับเหตุแบบสถานะ
(ไม่สวมหมวก = เห็นทั้งช่วง) แต่ใช้ไม่ได้กับเหตุแบบการกระทำที่เกิดกลางช่วง

**สิ่งที่ไม่เปลี่ยนเลย:** เวลา · ช่วง · ระดับความรุนแรง · กฎ · จำนวนเหตุการณ์ (11) · ตัวเลขความแม่นยำ
→ **UI ไม่ต้องแก้โค้ด** แค่ pull JSON ใหม่ รูปจะถูกเอง

**ของใหม่ในไฟล์:** event ที่ถูกแก้จะมีฟิลด์ `evidence_note` บอกเหตุผล (ไม่ต้องแสดงบน UI ก็ได้)

### เรื่องที่เพื่อนสงสัยว่า "เมื่อก่อนเวลามันตรง แล้วทำไมตอนนี้ไม่ตรง"

- **เวลา near-miss ไม่เคยเปลี่ยน** — เป็น `02:45–03:30` มาตั้งแต่ไฟล์แรกสุด (ตรวจ git ครบทุก commit แล้ว)
- **`02:55` ที่เคยเห็น** มาจากเอกสารเก่า (`HANDOFF.md`) ที่เขียนว่า *"near-miss @02:55 DETECTED"*
  ซึ่งเป็น **เวลาในเฉลย ไม่ใช่เวลาที่ระบบพ่น** — เอกสารนั้นติดป้ายว่าล้าสมัยแล้ว
- **ส่วนที่เปลี่ยนจริงคือตัวรูป** — เราสลับชุดเฟรมให้ตรงกับวิดีโอ ทำให้รูปที่ชื่อ `t0165.jpg`
  ขยับไปเสี้ยววินาที ของเดิมบังเอิญไปตกจังหวะที่แขนใกล้ตัวประกบพอดี **แต่ของเดิมไม่ตรงกับวิดีโอ**
